import 'package:flutter/material.dart';

class HomeBeauty extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Home Beauty'),
      ),
    );
  }
}
