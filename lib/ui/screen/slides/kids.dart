import 'package:flutter/material.dart';

class HomeKids extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Home Kids'),
      ),
    );
  }
}
