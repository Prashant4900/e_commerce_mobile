import 'package:flutter/material.dart';

class HomeCurve extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Home Curve'),
      ),
    );
  }
}
